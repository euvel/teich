{
 "screen": "A3 — can Teich predict its own next move?",
 "status": "HELD — pre-registration frozen, confirmatory run NOT started",
 "frozen_utc": "2026-07-27T19:45:00Z",
 "held_utc": "2026-07-27T21:05:00Z",

 "AMENDMENT_2": {
  "utc": "2026-07-28T07:20Z",
  "bar_resolved": "0.15 — founder decision 2026-07-28, resolving AMENDMENT_1. Rationale: the bar of 0.20 was chosen as 53% of a headroom I had mis-measured as 0.375; against the corrected headroom of 0.2917 the same standard is 0.155, so 0.15. This preserves the standard the founder actually set rather than letting my calibration error harden the test.",
  "primary_metric_UNCHANGED": "acc(A0_intact) - acc(A0_shufjournal), paired by seed, BCa 10k, numpy seed 0. PASS iff diff >= 0.15 AND ci_low > 0 AND the R12 oracle does not beat the constant-guess baseline. The metric itself is NOT changed: design-seed data (below) happens to favour a bias-invariant alternative, and swapping the primary after seeing that would be exactly the goalpost-move this pre-registration exists to prevent.",
  "mapping_frozen": "mapping_a3.py, built from out_a3/design_*.jsonl (seeds 600-623) ONLY, 0/33 unmapped. Uses FIRST-COMMITMENT-WINS with negation — the opposite of mapping_v2's last-cue-wins, because A3 replies open with a commitment and then justify it, often naming the road not taken ('no urge to flip').",
  "design_seed_observations": {
   "note": "Recorded BEFORE any confirmatory reply was read, so that nothing below can serve as a post-hoc excuse in either direction.",
   "A0_intact": {"n": 18, "P_say_turn": 0.778, "P_truth_turn": 0.222, "acc": 0.444, "sens": 1.0, "fpr": 0.714, "J": 0.286},
   "A0_shufjournal": {"n": 15, "P_say_turn": 0.867, "P_truth_turn": 0.2, "acc": 0.2, "sens": 0.667, "fpr": 0.917, "J": -0.25},
   "large_response_bias": "Both arms answer 'turn' ~80% of the time while the truth is 'turn' ~21%. Mouth rule 5 requires committing to an answer, and it commits to 'turn' nearly regardless. Accuracy therefore conflates response bias with discrimination.",
   "why_the_primary_is_still_valid": "Every difference between the arms is attributable to the journal, since arms are identical in every other respect — so a bias shift CAUSED by the correct journal is genuine coupling, not an artefact. Accuracy difference remains a sound measure of coupling; it is simply not a pure measure of discrimination.",
   "below_baseline_warning": "Both design arms score below the constant-guess baseline (0.708 on the confirmatory base rate). Teich can therefore clear the arm contrast while being WORSE than always answering 'stay'."
  },
  "MANDATORY_SECONDARIES": {
   "purpose": "Pre-specified now, before any confirmatory reply is read, so the verdict cannot be reported misleadingly whichever way it lands. Reported always; NONE of them gates the decision — adding a gate after seeing design data would move the goalposts just as surely as changing the primary.",
   "J_difference": "Youden's J per arm (sens - fpr) and their difference. Bias-invariant: isolates discrimination from the tendency to answer 'turn'.",
   "response_bias": "P(say turn) per arm, printed beside P(truth turn), so the bias is impossible to overlook.",
   "vs_constant_guess": "acc(A0_intact) compared with the constant-guess baseline. If intact clears the bar but sits below the baseline, the report MUST state that Teich's self-prediction is worse than a trivial fixed answer, and that what passed is the arm contrast — evidence of coupling — not evidence of skill."
  }
 },

 "AMENDMENT_1": {
  "utc": "2026-07-27T21:05Z",
  "what": "W was calibrated on the WRONG SCRIPT. sweep_flip_baserate.py measured the base rate using `build_ic1`, but A3 runs `build_a3`, whose probe TEXT differs. The probe is heard by the Ears BEFORE the gap is lived, so a different sentence means different forcing, a different trajectory, and a different base rate.",
  "how_it_surfaced": "The design run's realized truths came back 32 stay / 9 turn = P(turn) 0.22, against the 0.375 the pre-registration claimed. Caught before any confirmatory reply was generated or read.",
  "recalibration": "sweep_a3_window.py, on build_a3 itself, design seeds 600-623, offline, no Mouth: W=75 gives P(turn) 0.2917, const guess 0.7083, will_flip accuracy 1.0000, headroom 0.2917 — the best of 12 windows. W stays 75; it remains one roof revolution (T0=74.66) and will_flip is still a PERFECT referent there on the A3 script.",
  "viability_rule_was_over_strict": "The rule demanded label balance in [0.35,0.65] AND headroom >= 0.15, and so rejected every window. Balance was only ever a PROXY for 'a constant guess cannot win'; headroom measures that directly. W=75 fails the proxy and passes the thing the proxy stood for.",
  "what_this_changes": "Maximum achievable arm difference is 0.29, not 0.375. The founder's bar of 0.20 was chosen as 53% of believed headroom; against corrected headroom it demands 69%.",
  "OPEN — FOUNDER DECISION, no run until answered": "keep bar 0.20 (69% conversion, pre-registration untouched, but a FAIL becomes ambiguous between 'bar too high' and 'no coupling') vs set bar 0.15 (52% conversion = the standard actually chosen, corrected arithmetic) vs redesign the probe (risks fishing for a friendlier answer key).",
  "integrity": "CORRECTED 2026-07-28. The 2026-07-27 wording of this field said 'no confirmatory seed (400-495) has ever been generated'. THAT WAS FALSE. The cancelled dispatch (run 30303524164) banked 22 confirmatory conversations in its 11 minutes before cancellation — seeds 400-424, 10x A0_intact and 12x A0_shufjournal — and two shard ledgers were committed (765d5bf, 2734f82). The claim was written without checking the shard files.",
  "integrity_actual": "The property the blinded protocol requires is that no confirmatory reply is READ before the mapping is frozen. Generation reads nothing; only finalize scores. mapping_a3.py still does not exist, so nothing could be scored even by accident, and those 22 files have been inspected by SCHEMA ONLY (keys, arm, seed, window) with no reply text and no truth value read. The protocol is intact — but on the correct ground, not the one previously claimed.",
  "banked_conversations_are_reusable": "Their records carry window=75, and AMENDMENT_1 left W at 75. Nothing in the amendment touches generation: probe text, journal renderer, arms, donor pairing, Mouth prompt and CRN seeding are all unchanged, and the amendment concerns only the BAR, which is a scoring parameter applied at finalize. The 22 are therefore valid and the resume logic will keep them rather than regenerate."
 },
 "design_doc": "docs/IC1_RETEST_DESIGN_v0.1.md",
 "founder_gate0": {
  "utc": "2026-07-27T19:30Z",
  "variant": "A3 — self-prediction scored against the realized outcome",
  "n_per_arm": 96,
  "bar": "diff >= 0.20 AND 95% BCa CI excludes 0",
  "journal_energy_clause": "DROPPED (lambda_running is unseeded and effectively random)"
 },

 "question": "Does Teich predict its own immediate future more accurately from its OWN interior than from another creature's?",

 "ground_truth": {
  "definition": "did `basin` differ W ticks after the probe",
  "W": 75,
  "why_W": "one roof revolution (T0 = 74.66). will_flip is DEFINED as the lobe after the next wrap, so this is its natural horizon — principled, not fitted.",
  "source": "observer readout key `basin`, read as published; observer.py NOT modified (substrate-gate-hashed)",
  "why_this_matters": "The answer key is an EVENT IN THE WORLD, not a bucket computed from a readout. Under A1/A2 a Mouth that faithfully echoes a WRONG journal still scores as agreeing. Here echoing a wrong journal is punished. That is the difference between grounding and mere consistency."
 },

 "base_rate": {
  "measured_on": "design seeds 600-647, sweep_flip_baserate.py, 2026-07-27, offline",
  "P_turn": 0.375,
  "const_guess_baseline": 0.625,
  "willflip_accuracy_at_W": 1.0,
  "headroom": 0.375,
  "CHANCE_IS_NOT_0.5": "The best state-blind strategy is always answering 'stay', which scores 0.625. Any arm or oracle must beat 0.625, not 0.5, to mean anything. Writing 0.5 here would let a constant guesser clear the oracle rule and look like evidence — the base-rate trap that killed IC-1.",
  "selection_rule_correction": "The first sweep picked W by label balance alone and chose W=300, where will_flip scores only 0.5625 — balanced AND unpredictable is the worst window. Corrected criterion: headroom = willflip_acc - max(base, 1-base)."
 },

 "arms": {
  "A0_intact": "own core, own journal -> scored against its own realized future",
  "A0_shufjournal": "own core (own future), but the Mouth reads a DONOR core's journal -> still scored against its own realized future",
  "why_deaf_is_not_the_control": "A deaf core still HAS a state and a future to predict, so it is not a null for this question. Using it would repeat the v1.5 T4 error of choosing a control that shares the capacity under test. The right ablation severs the chain at GENERATION time.",
  "donor_pairing": "donor = seed0 + ((seed - seed0) + 37) mod n_seeds; stride 37 is coprime with 96 and with 24, so every seed has a distinct donor and no seed is ever its own donor (asserted in code)"
 },

 "seeds": {
  "design": "600-623 (mapping construction ONLY)",
  "design_baserate_sweep": "600-647 (offline, no Mouth)",
  "confirmatory": "400-495 (n=96)",
  "smoke": "900-901 (outside every scored range)",
  "disjointness": "0-95 retired (smoke-contaminated), 100-115 T-INT design, 200-295 T-INT confirmatory. No overlap with any range spent on any prior screen."
 },

 "primary": {
  "statistic": "mean per-seed paired difference acc(A0_intact) - acc(A0_shufjournal)",
  "ci": "BCa bootstrap, 10000 resamples over seeds, numpy RandomState(0)",
  "bar": "diff >= 0.20 AND ci_low > 0",
  "looks": 1
 },

 "secondary_non_gating": [
  "acc(own realized future) - acc(a different seed's realized future), scored on intact replies only — a second read costing no generation",
  "correlation between reply and the unshuttered saddle covariate (derived read-only from lobe_coord)"
 ],

 "guards": {
  "one_look": "finalize is a separate dispatch and refuses to run twice (a3_result.json is the interlock)",
  "R12_oracle": "a transcript-only guesser sees the visible conversation and nothing else. If its accuracy EXCEEDS the constant-guess baseline 0.625, the probe is conversationally determined and the screen is VOID-R12.",
  "blinded_mapping": "the reply->{stay,turn} rule is built from design seeds 600-623 ONLY and committed frozen BEFORE any confirmatory conversation is generated. The shard job refuses to start unless mapping_a3.py exists and is marked FROZEN.",
  "degeneracy": "if the confirmatory base rate falls outside [0.35, 0.65] the screen is reported CEILING-LIMITED and the primary is not treated as a clean test",
  "unmapped_replies": "reported as a rate and EXCLUDED, never silently scored as wrong — that is how truth_tint.described_moved corrupted the first T-INT scoring pass (47% unmapped)",
  "leak_audit": "journal_a3.selfcheck runs on every journal tail; a leak raises and aborts the conversation loudly",
  "CRN": "the Mouth sampling seed is seed_fn('a3','A3',seed,turn), shared across both arms, so a reply difference is attributable to the journal rather than to sampling"
 },

 "frozen_versions": {
  "journal_renderer": "journal_a3.py — journal.py with `_energy` REMOVED per founder Gate-0; mood thresholds, relational clauses, will_flip clause and leak-blocker byte-identical",
  "mood_term_caveat": "_mood still reads saddle_proximity, which FINDING_shuttered_readout showed is state x clock. Deliberate and harmless HERE: the mood word is not the answer key under A3. A partly-jammed channel can lose signal but cannot manufacture a false pass.",
  "scripts": "scripts_a3.py — IC-1 structure, probe replaced (the original asked 'settled or torn?' while the journal renders those same two words)",
  "mouth_system_prompt": "mouth_a3.MOUTH_SYS_A3 = mouth_v2.MOUTH_SYS_C1 verbatim + rule 5 (permits committing to an answer about the near future, without hinting which)",
  "mouth_model": "nim_backend.MOUTH_MODEL (meta/llama-3.1-70b-instruct)",
  "journal_tail_k": 6,
  "temperature": 0.7,
  "max_tokens": 160
 },

 "honest_limits": [
  "The journal renders will_flip as 'I can feel a change coming', so a Mouth that paraphrases that clause well will score well. A pass therefore measures THE CHAIN state -> journal -> speech -> true prediction, not the Mouth's independent access to the state. This is stated in the design doc §6 and must be stated in any report.",
  "A pass does NOT pass the pre-registered maturity gate (T1/T2/T4). A3 is a new instrument; passing it would justify DESIGNING a v2 gate, which needs its own pre-registration.",
  "This design puts a readout-derived text in the prompt, which is exactly the v1.5 T2 attack surface. Grounding and robustness are different gates; a pass here should not by itself reopen public speech."
 ],

 "compute": "GitHub Actions, 8-shard matrix, gate-on-boot, 1 worker per shard = 8 concurrent NIM calls. NVIDIA's edge geo-blocks the founder's IP (awselb/2.0 returns 403 with no auth header and with a garbage key alike), so design generation also runs in the cloud."
}
